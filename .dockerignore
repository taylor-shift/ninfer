# COPY . . in deploy/multi-gpu/Dockerfile brings the checkout into the builder;
# keep local build output and VCS metadata out so the layer stays small and
# cache-stable.
.git
build/
*/build/
.cache/
*.ninfer
*.tgz
